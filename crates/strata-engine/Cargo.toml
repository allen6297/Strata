[package]
name = "strata-engine"
version = "0.1.0"
edition = "2021"
description = "Strata 2D/3D scene runtime (custom engine core)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
